import {useEffect,useState} from 'react';import api from '../api';import ProductCard from '../components/ProductCard';
export default function Home(){const [products,setProducts]=useState([]);const [categories,setCategories]=useState([]);const [q,setQ]=useState('');const [category,setCategory]=useState('');const [sort,setSort]=useState('');const [loading,setLoading]=useState(true);useEffect(()=>{api.get('/products/categories').then(r=>setCategories(r.data)).catch(()=>{});},[]);useEffect(()=>{setLoading(true);api.get('/products',{params:{search:q,category,sort}}).then(r=>setProducts(r.data)).finally(()=>setLoading(false));},[q,category,sort]);return <><section className="hero"><div><p className="eyebrow">LEVEL 3 · TASK 1</p><h1>Everything you want.<br/><span>One smart store.</span></h1><p>Discover curated products with secure checkout and a frictionless shopping experience.</p></div><div className="hero-card">🛍️<strong>ShopSphere</strong><small>Modern MERN-style commerce</small></div></section><section className="toolbar"><input placeholder="Search products..." value={q} onChange={e=>setQ(e.target.value)}/><select value={category} onChange={e=>setCategory(e.target.value)}><option value="">All categories</option>{categories.map(c=><option key={c}>{c}</option>)}</select><select value={sort} onChange={e=>setSort(e.target.value)}><option value="">Featured</option><option value="price-asc">Price: low to high</option><option value="price-desc">Price: high to low</option><option value="newest">Newest</option></select></section>{loading?<div className="center">Loading products...</div>:<section className="grid">{products.map(p=><ProductCard key={p._id} product={p}/>)}</section>}{!loading&&!products.length&&<div className="empty">No products match your filters.</div>}</>}
