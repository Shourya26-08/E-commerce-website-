import {useEffect,useState} from 'react';import {Link} from 'react-router-dom';import api from '../api';
export default function Orders(){const [orders,setOrders]=useState([]);useEffect(()=>{api.get('/orders').then(r=>setOrders(r.data));},[]);return <section><h1>Your orders</h1>{!orders.length?<div className="empty">No orders yet. <Link to="/">Start shopping</Link></div>:<div className="orders">{orders.map(o=><article className="order" key={o._id}><div><b>Order #{o._id.slice(-8).toUpperCase()}</b><span>{new Date(o.createdAt).toLocaleString()}</span></div><div><span>{o.items.length} item(s)</span><b>${o.total.toFixed(2)}</b><span className="status">{o.status}</span></div></article>)}</div>}</section>}
