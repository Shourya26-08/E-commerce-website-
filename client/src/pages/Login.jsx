import {useState} from 'react';import {Link,useNavigate} from 'react-router-dom';import {useAuth} from '../context/AuthContext';
export default function Login(){const {login}=useAuth();const nav=useNavigate();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const submit=async e=>{e.preventDefault();try{await login(email,password);nav('/');}catch(err){setError(err.response?.data?.message||'Login failed')}};return <AuthForm title="Welcome back" subtitle="Sign in to continue shopping." onSubmit={submit} error={error}><input type="email" placeholder="Email" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button className="large-btn">Sign in</button><p>New here? <Link to="/register">Create an account</Link></p></AuthForm>}
function AuthForm({title,subtitle,onSubmit,error,children}){return <section className="auth"><h1>{title}</h1><p>{subtitle}</p>{error&&<div className="error">{error}</div>}<form onSubmit={onSubmit}>{children}</form></section>}
